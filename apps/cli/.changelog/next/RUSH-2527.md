- **Name and attach accounts without relationship flags.** `agents accounts name <agent@version> <name>` records native login metadata without copying OAuth credentials; `attach <account> <target>`, `detach`, and `view` bind the same named identity to native installations, custom harnesses, profiles, routines, and remote runs. Provider bundles remain explicit, prompt-free portable credentials and now sync with `accounts sync <account> <device>`.
