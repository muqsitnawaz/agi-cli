- **`main` was red on Windows CI; two POSIX-only tests are now quarantined on win32.** `manualUninstallCommand`'s tests from RUSH-2705 assert a hardcoded POSIX nvm path (`/home/u/.nvm/versions/node/v24.15.0/...`) and POSIX shell output (`rm -rf '<path>'`). On win32 the function resolves a Windows layout and quotes for PowerShell, so both assert a platform they never run on. They now carry `it.skipIf(process.platform === 'win32')` with a stated reason — the same RUSH-2215 quarantine pattern already used three times in that file, and the one the Tests workflow's own comment prescribes. The Windows job only runs for diffs touching windows-sensitive paths (`ci-scope.ts`: `lib/hooks`, `lib/platform/`, `shims*`, `binary-shadow`, `lib/hosts/`), which is why this stayed invisible on the PR that introduced it and surfaced on an unrelated one. Test-only change; `manualUninstallCommand` itself is untouched.
