- **`agents sessions trace` reads Bash by its actual command.** Each shell step now carries the effective `program` it ran — `git`, `gh`, `agents`, `bun`, `sed` (via the shared `extractShellPrograms` parser, wrappers like `timeout`/`sudo` and bare `cd`/`export` unwrapped) — plus the process `exitCode` when the harness records one. The `--text` view and `--json` envelope now label steps by program and break the command mix down that way (`git 92  agents 80  gh 69` instead of a single "Bash 92%"), and show `exit N` on failures. Additive `--json` step fields (`program`, `exitCode`); the HTML analysis-hero redesign that consumes them follows. Source: `apps/cli/src/lib/session/trajectory.ts`.
