- **Un-addressable live sessions now print a recovery command (`agents sessions resume <id>` / tmux wrap) instead of a reason-only skip (PHNX-3070).**
  `agents sessions focus`, `agents go`, `agents message`, `agents sessions inject`, and the watchdog append `addressabilityRecoveryHint` at the point of use so a live session with no inject rail is recoverable. The indexed session id is used even when the live row has not registered one yet (the IDE-terminal post-spawn case). Source: `cli/src/lib/terminal/resolve.ts`.

